// New thread track's screens.
extension FixtureCatalog {
    static var newThread: [FixtureScreen] {
        [
            FixtureScreen(name: "newthread", presented: .newThread(.compose(host: nil))),
        ]
    }
}

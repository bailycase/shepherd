name: Release

# One workflow ships two apps. scripts/release.py holds the rules (tested in
# Tests/Release); this file only runs them.
#
#   tag vX.Y.Z          → Shepherd, stable   (appcast.xml)
#   tag vX.Y.Z-beta.N   → Shepherd, beta     (appcast-beta.xml — beta + stable items)
#   push to nightly     → Shepherd Nightly   (appcast-shepherd-nightly.xml)
#   tag vX.Y.Z-rc.N     → nothing: release candidates are retired
#   manual run          → as above for its ref; any branch but nightly builds nothing
#
# Shepherd Nightly is its own app (com.bailycase.shepherd.nightly, Shepherd
# Nightly.app, Shepherd-Nightly.dmg), so it installs beside Shepherd and only
# ever reads its own feed. Builds already in the field still read
# appcast-rc.xml and appcast-nightly.xml: both are the beta feed with its items
# untagged, which every such build sees, so they update to a Shepherd build
# that moves them to Beta, and never to Shepherd Nightly.
#
# Each build is signed, notarized and packaged into a DMG, published as a
# GitHub Release, and every appcast on gh-pages is regenerated. Signing is
# Developer ID + notarization when the secrets exist, ad-hoc otherwise.
#
# Secrets (all optional until Developer ID exists):
#   SPARKLE_PRIVATE_KEY   EdDSA private key from `generate_keys -x` (one key for both apps)
#   DEVELOPER_ID_P12      base64 .p12 of the Developer ID Application cert
#   DEVELOPER_ID_P12_PASSWORD
#   NOTARY_KEY_ID / NOTARY_ISSUER_ID / NOTARY_KEY  App Store Connect API key
#
# No swift test step: CI already ran `swift test` on the commit being tagged.
on:
  push:
    tags: ["v*"]
    branches: [nightly]
  workflow_dispatch:

permissions:
  contents: write

# A newer nightly push supersedes a queued/running one; tags never cancel.
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') }}

jobs:
  plan:
    name: plan
    runs-on: ubuntu-latest
    outputs:
      plan: ${{ steps.plan.outputs.plan }}
    steps:
      - uses: actions/checkout@v4
      - name: Test the release rules
        run: python3 -m unittest discover -s Tests/Release -v
      - name: Plan
        id: plan
        run: |
          # A re-run keeps the run number (the build number), so release.py
          # refuses a nightly re-run whose commit already has a nightly tag.
          published=""
          if [[ "$GITHUB_RUN_ATTEMPT" != 1 ]]; then
            published=$(git ls-remote --tags origin 'refs/tags/nightly-*' \
              | awk -v sha="$GITHUB_SHA" '$1 == sha { sub("refs/tags/", "", $2); print $2; exit }')
          fi
          plan=$(python3 scripts/release.py plan "$GITHUB_REF" "$(date -u +%Y%m%d%H%M)" \
            "$GITHUB_RUN_ATTEMPT" "$published")
          echo "$plan" >> "$GITHUB_OUTPUT"
          jq -r 'if .build then "\(.title): \(.scheme), \(.configuration), \(.dmg)" else "::notice::\(.reason)" end' <<<"${plan#plan=}"

  release:
    name: build and publish
    needs: plan
    if: fromJSON(needs.plan.outputs.plan).build
    runs-on: macos-26
    env:
      APP_KEY: ${{ fromJSON(needs.plan.outputs.plan).app }}
      APP_NAME: ${{ fromJSON(needs.plan.outputs.plan).name }}
      CHANNEL: ${{ fromJSON(needs.plan.outputs.plan).channel }}
      VERSION: ${{ fromJSON(needs.plan.outputs.plan).version }}
      TAG: ${{ fromJSON(needs.plan.outputs.plan).tag }}
      SCHEME: ${{ fromJSON(needs.plan.outputs.plan).scheme }}
      CONFIGURATION: ${{ fromJSON(needs.plan.outputs.plan).configuration }}
      PRODUCTS: build/Build/Products/${{ fromJSON(needs.plan.outputs.plan).configuration }}
      PRODUCT: ${{ fromJSON(needs.plan.outputs.plan).product }}
      DMG: ${{ fromJSON(needs.plan.outputs.plan).dmg }}
      DSYMS: ${{ fromJSON(needs.plan.outputs.plan).dsyms }}
      VOLUME: ${{ fromJSON(needs.plan.outputs.plan).volume }}
      TITLE: ${{ fromJSON(needs.plan.outputs.plan).title }}
      PRERELEASE: ${{ fromJSON(needs.plan.outputs.plan).prerelease }}
      NOTES: ${{ fromJSON(needs.plan.outputs.plan).notes }}
    steps:
      - uses: actions/checkout@v4

      - name: Xcode version
        id: xcode
        run: echo "version=$(xcodebuild -version | shasum -a 256 | cut -c1-12)" >> "$GITHUB_OUTPUT"
      - name: Cache DerivedData
        uses: actions/cache@v4
        with:
          path: build
          # Exact-match only. A prefix-restored cache from before a model
          # change poisons incremental builds (stale ShepherdCore objects vs
          # new ShepherdApp sources — nightly 0217 shipped a build break that
          # `swift test` had already passed). Release builds are rare enough
          # that a clean rebuild beats a wrong binary. Keyed per configuration:
          # the two apps build different ones.
          key: xcode-${{ runner.os }}-${{ env.CONFIGURATION }}-${{ steps.xcode.outputs.version }}-${{ hashFiles('Package.resolved', 'Sources/**/*.swift', 'App/**/*.swift') }}

      - name: Import Developer ID certificate
        id: cert
        env:
          P12: ${{ secrets.DEVELOPER_ID_P12 }}
          P12_PASSWORD: ${{ secrets.DEVELOPER_ID_P12_PASSWORD }}
        run: |
          if [[ -z "$P12" ]]; then
            echo "identity=-" >> "$GITHUB_OUTPUT"
            echo "No Developer ID cert; ad-hoc signing."
            exit 0
          fi
          KEYCHAIN=$RUNNER_TEMP/build.keychain
          security create-keychain -p ci "$KEYCHAIN"
          security set-keychain-settings -lut 21600 "$KEYCHAIN"
          security unlock-keychain -p ci "$KEYCHAIN"
          echo "$P12" | base64 -d > "$RUNNER_TEMP/cert.p12"
          security import "$RUNNER_TEMP/cert.p12" -k "$KEYCHAIN" -P "$P12_PASSWORD" \
            -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple: -s -k ci "$KEYCHAIN"
          security list-keychains -d user -s "$KEYCHAIN" login.keychain
          IDENTITY=$(security find-identity -v -p codesigning "$KEYCHAIN" \
            | awk -F'"' '/Developer ID Application/ {print $2; exit}')
          echo "identity=$IDENTITY" >> "$GITHUB_OUTPUT"

      - name: Build ${{ env.APP_NAME }}
        run: |
          xcodebuild \
            -project Shepherd.xcodeproj \
            -scheme "$SCHEME" \
            -configuration "$CONFIGURATION" \
            -destination 'platform=macOS,arch=arm64' \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            MARKETING_VERSION="$VERSION" \
            CURRENT_PROJECT_VERSION='${{ github.run_number }}' \
            build

      # The one guard against shipping one app under the other's identity:
      # bundle id, name, executable and feed must be this app's.
      - name: Verify the app's identity
        run: python3 scripts/release.py verify-app "$PRODUCTS/$PRODUCT" "$APP_KEY" "$VERSION"

      # Workflow artifacts expire (90 days at most on a public repo), while a
      # stable release stays on the appcast far longer, so the same dSYMs are
      # also zipped for the GitHub release. Neither may fail a release.
      - name: Keep dSYMs
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: dSYMs-${{ env.TAG }}
          path: ${{ env.PRODUCTS }}/*.dSYM
          if-no-files-found: warn

      - name: Zip dSYMs
        continue-on-error: true
        working-directory: ${{ env.PRODUCTS }}
        run: zip -qr "$GITHUB_WORKSPACE/$DSYMS" ./*.dSYM

      # `xcodebuild build` never strips, so the app binary would ship a debug
      # map of runner object-file paths. -S -x drops debug and local symbols
      # and keeps global ones, so crash logs stay partly readable; the dSYMs
      # above predate the strip and share its UUIDs, so they still symbolicate.
      - name: Strip debug symbols
        run: xcrun strip -S -x "$PRODUCTS/$PRODUCT/Contents/MacOS/"*

      - name: Sign
        run: |
          scripts/sign-app.sh "$PRODUCTS/$PRODUCT" \
            '${{ steps.cert.outputs.identity }}' App/Shepherd.entitlements

      - name: Notarize
        if: steps.cert.outputs.identity != '-'
        env:
          NOTARY_KEY_ID: ${{ secrets.NOTARY_KEY_ID }}
          NOTARY_ISSUER_ID: ${{ secrets.NOTARY_ISSUER_ID }}
          NOTARY_KEY: ${{ secrets.NOTARY_KEY }}
        run: |
          APP="$PRODUCTS/$PRODUCT"
          echo "$NOTARY_KEY" > "$RUNNER_TEMP/notary.p8"
          ditto -c -k --sequesterRsrc --keepParent "$APP" notarize.zip
          xcrun notarytool submit notarize.zip \
            --key "$RUNNER_TEMP/notary.p8" \
            --key-id "$NOTARY_KEY_ID" \
            --issuer "$NOTARY_ISSUER_ID" \
            --wait
          xcrun stapler staple "$APP"

      - name: Package DMG
        env:
          NOTARY_KEY_ID: ${{ secrets.NOTARY_KEY_ID }}
          NOTARY_ISSUER_ID: ${{ secrets.NOTARY_ISSUER_ID }}
        run: |
          STAGE=$RUNNER_TEMP/dmg
          mkdir -p "$STAGE"
          cp -R "$PRODUCTS/$PRODUCT" "$STAGE/"
          ln -s /Applications "$STAGE/Applications"
          hdiutil create -volname "$VOLUME" -srcfolder "$STAGE" -ov -format UDZO "$DMG"
          # The stapled app is inside. The DMG gets its own notarization
          # ticket so Gatekeeper can verify it offline at mount time; stapler
          # has nothing to staple until the DMG itself is notarized. Ad-hoc
          # builds skip all of it.
          if [[ '${{ steps.cert.outputs.identity }}' != '-' ]]; then
            codesign --force --timestamp --sign '${{ steps.cert.outputs.identity }}' "$DMG"
            # notary.p8 was written by the Notarize step.
            xcrun notarytool submit "$DMG" \
              --key "$RUNNER_TEMP/notary.p8" \
              --key-id "$NOTARY_KEY_ID" \
              --issuer "$NOTARY_ISSUER_ID" \
              --wait
            xcrun stapler staple "$DMG"
          fi
          shasum -a 256 "$DMG"

      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          ARGS=(--title "$TITLE" --notes "$NOTES")
          # beta and nightly are GitHub prereleases; feed membership is by tag
          # NAME (release.py route), never by the prerelease flag — so the
          # nightly cleanup matches names too, or it would eat betas.
          if [[ "$PRERELEASE" == "true" ]]; then
            ARGS+=(--prerelease)
          fi
          if [[ "$CHANNEL" == "nightly" ]]; then
            # Keep only the latest few nightlies. Those from before Shepherd
            # Nightly (Shepherd.dmg only) age out the same way.
            for old in $(gh release list --limit 100 --json tagName \
              --jq '.[] | select(.tagName | startswith("nightly-")) | .tagName' | tail -n +4); do
              gh release delete "$old" --cleanup-tag --yes || true
            done
            git tag "$TAG" && git push origin "$TAG"
          fi
          gh release create "$TAG" "$DMG" "${ARGS[@]}"
          if [[ -f "$DSYMS" ]]; then
            gh release upload "$TAG" "$DSYMS" \
              || echo "::warning::Could not attach dSYMs to $TAG."
          fi

      - name: Update appcasts
        env:
          GH_TOKEN: ${{ github.token }}
          SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY }}
        run: |
          if [[ -z "$SPARKLE_PRIVATE_KEY" ]]; then
            echo "No SPARKLE_PRIVATE_KEY secret; skipping appcast." && exit 0
          fi
          BIN=$(find ~/Library/Developer/Xcode/DerivedData build .build \
            -path '*artifacts*Sparkle/bin' -type d 2>/dev/null | head -1)
          if [[ -z "$BIN" ]]; then
            # Resolve tools straight from the package if the build cache moved.
            swift package resolve
            BIN=$(find .build -path '*artifacts*Sparkle/bin' -type d | head -1)
          fi
          echo "$SPARKLE_PRIVATE_KEY" > "$RUNNER_TEMP/sparkle_key"

          # The appcasts are rebuilt from every published release so they
          # always reflect the full history. release.py route says which feed
          # directories each release lands in and which asset feeds them: a
          # stable DMG also lands in beta (the superset), and a nightly-*
          # release feeds Shepherd Nightly only through Shepherd-Nightly.dmg.
          gh release list --limit 100 --json tagName --jq '.[].tagName' > "$RUNNER_TEMP/tags"
          while read -r dir channel; do mkdir -p "casts/$dir"; done < <(python3 scripts/release.py feeds)
          python3 scripts/release.py route < "$RUNNER_TEMP/tags" \
          | while read -r tag asset archive feeds; do
            IFS=, read -r -a dirs <<<"$feeds"
            first="casts/${dirs[0]}"
            gh release download "$tag" -p "$asset" -O "$first/$archive" </dev/null || continue
            for dir in "${dirs[@]:1}"; do
              cp "$first/$archive" "casts/$dir/"
            done
          done

          # generate_appcast errors on an empty directory (e.g. no stable
          # releases yet); an empty channel keeps an empty feed instead.
          empty_feed() {
            printf '<?xml version="1.0" encoding="utf-8"?>\n<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle"><channel><title>Shepherd</title></channel></rss>\n' > "$1"
          }
          # One generate_appcast run per feed directory. A tagged feed marks
          # every item with its channel (the client allows exactly that one).
          # -o: left alone, generate_appcast names the file after the app's
          # SUFeedURL. compgen, not ls: `ls *.dmg` fails when the glob is
          # unmatched. Then point each archive at its tag's asset, and upload
          # the feed's deltas (small binary patches to the newest build) to its
          # newest release so Sparkle can update with a patch instead of the
          # DMG. release.py deltas renames them first: Shepherd Nightly's are
          # named after the app, with a space GitHub would rewrite. Commands
          # inside the loops read /dev/null, never the list being looped over.
          while read -r dir channel; do
            args=(--ed-key-file "$RUNNER_TEMP/sparkle_key" \
              --download-url-prefix "https://github.com/${{ github.repository }}/releases/download/" \
              --link "https://github.com/${{ github.repository }}" \
              -o "casts/$dir/appcast.xml")
            [[ "$channel" != "-" ]] && args+=(--channel "$channel")
            if compgen -G "casts/$dir/*.dmg" >/dev/null; then
              "$BIN/generate_appcast" "${args[@]}" "casts/$dir" </dev/null
            else
              empty_feed "casts/$dir/appcast.xml"
            fi
            python3 scripts/release.py fix-urls "casts/$dir/appcast.xml" "$dir"
            latest=$(python3 scripts/release.py latest "$dir" < "$RUNNER_TEMP/tags")
            if [[ -n "$latest" ]]; then
              deltas=()
              while IFS= read -r delta; do deltas+=("$delta"); done \
                < <(python3 scripts/release.py deltas "casts/$dir" "$latest")
              if [[ ${#deltas[@]} -gt 0 ]]; then
                gh release upload "$latest" "${deltas[@]}" --clobber </dev/null
              fi
            fi
          done < <(python3 scripts/release.py feeds)

          git config user.name github-actions
          git config user.email github-actions@github.com
          git fetch origin gh-pages || true
          if git rev-parse --verify origin/gh-pages >/dev/null 2>&1; then
            git worktree add pages origin/gh-pages
          else
            git worktree add --orphan -b gh-pages pages
          fi
          # Every feed, plus appcast-rc.xml and appcast-nightly.xml for the
          # Shepherd builds already installed that still read them.
          written=()
          while IFS= read -r file; do written+=("$file"); done \
            < <(python3 scripts/release.py publish casts pages)
          cd pages
          git add "${written[@]}"
          git commit -m "appcast: $TAG" || exit 0
          git push origin HEAD:gh-pages

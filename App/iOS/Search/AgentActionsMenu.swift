import SwiftUI

/// The agent actions in a thread's options menu (search track): Rename and Delete. Menu items
/// only; the thread's menu holds them.
struct AgentActionsMenu: View {
    let thread: AgentRef

    var body: some View {
        EmptyView()
    }
}
